version: 1.0.0
target_crate: copia
bindings:
- contract: delta-sync-v1.yaml
  equation: rolling_checksum
  module_path: copia::checksum::FastRollingChecksum
  function: update
  signature: 'fn update(&mut self, old_byte: u8, new_byte: u8)'
  status: implemented
  notes: Adler-32 variant with O(1) sliding window; a and b always reduced mod 65521
- contract: delta-sync-v1.yaml
  equation: delta_computation
  module_path: copia::sync::CopiaSync::delta
  function: delta
  signature: 'fn delta<R: Read>(&self, source: R, signature: &Signature) -> Result<Delta>'
  status: implemented
  notes: Scans source with rolling checksum, emits Copy/Literal ops
- contract: delta-sync-v1.yaml
  equation: patch_apply
  module_path: copia::sync::CopiaSync::patch
  function: patch
  signature: 'fn patch<R: Read, W: Write>(&self, basis: R, delta: &Delta, output: &mut W) -> Result<()>'
  status: implemented
  notes: Applies Copy and Literal ops to reconstruct source from basis + delta
critical_path:
- rolling_checksum
- delta_computation
- patch_apply
