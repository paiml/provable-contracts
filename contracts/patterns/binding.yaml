version: 1.0.0
registry: true
description: >
  Cross-cutting contract patterns from Section 34 (Systems Contract Patterns).
  These are abstract patterns not bound to specific functions. Individual repos
  bind these patterns to concrete implementations via their own binding.yaml.
contracts:
- threading-safety-v1.yaml
- async-safety-v1.yaml
- compute-parity-v1.yaml
- transpiler-correctness-v1.yaml
bindings: []
