version: 1.0.0
target_crate: rclean
bindings:
- contract: cleanup-safety-v1.yaml
  equation: duplicate_detection
  module_path: rclean
  function: find_duplicates
  signature: 'fn find_duplicates(files: &[PathBuf]) -> Vec<DuplicateGroup>'
  status: implemented
  notes: MD5 hash-based parallel duplicate detection using rayon
- contract: cleanup-safety-v1.yaml
  equation: scan_completeness
  module_path: rclean
  function: walk_directory
  signature: 'fn walk_directory(root: &Path, options: &WalkOptions) -> DataFrame'
  status: implemented
  notes: Uses ignore + walkdir with configurable depth, hidden files, gitignore
- contract: cleanup-safety-v1.yaml
  equation: outlier_detection
  module_path: rclean::outliers
  function: detect_outliers
  signature: 'fn detect_outliers(files: &DataFrame) -> Vec<OutlierFile>'
  status: implemented
  notes: IQR-based outlier detection with linfa clustering support
critical_path:
- scan_completeness
- duplicate_detection
- outlier_detection
