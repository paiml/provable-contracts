version: 1.0.0
target_crate: pzsh
bindings:
- contract: shell-execution-v1.yaml
  equation: startup_budget
  module_path: pzsh::shell
  function: init
  signature: 'fn init(config: &ShellConfig) -> Result<Shell>'
  status: implemented
  notes: Hard 10ms startup constraint; uses quanta for precise timing
- contract: shell-execution-v1.yaml
  equation: parser_correctness
  module_path: pzsh::parser
  function: parse
  signature: 'fn parse(input: &str) -> Result<Command, ParseError>'
  status: implemented
  notes: POSIX-compatible parser with 2ms time budget
- contract: shell-execution-v1.yaml
  equation: config_validation
  module_path: pzsh::config
  function: load
  signature: 'fn load(path: &Path) -> Result<ShellConfig, ConfigError>'
  status: implemented
  notes: TOML config with forbidden pattern detection
critical_path:
- config_validation
- startup_budget
- parser_correctness
