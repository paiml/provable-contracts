metadata:
  version: 1.0.0
  description: Generic display-format contract — common Rust API pattern
  references:
  - 'Rust API Guidelines: https://rust-lang.github.io/api-guidelines/'
equations:
  display_format:
    formula: display-format follows standard Rust conventions
    invariants:
    - Type safety preserved
    - No panics on valid input
    preconditions:
    - 'input.len() > 0'
    - width > 0
    postconditions:
    - 'result.len() > 0'
    lean_theorem: Theorems.Display_Format
  render:
    formula: render transforms data into displayable output
    invariants:
    - Type safety preserved
    - No panics on valid input
    preconditions:
    - 'input.len() > 0'
    postconditions:
    - 'result.len() > 0'
    lean_theorem: Theorems.Render
proof_obligations:
- type: invariant
  property: display-format correctness
falsification_tests:
- id: FALSIFY-GEN-001
  rule: display-format contract
  prediction: Function follows display-format pattern
  if_fails: Implementation deviates from pattern
kani_harnesses:
- id: KANI-GEN-001
  obligation: display-format correctness
  bound: 8
  strategy: bounded_int
  solver: cadical
qa_gate:
  id: F-DFV-001
  name: display-format-v1 Contract
  description: Quality gate for Generic display-format contract — common Rust API
    pattern
  checks:
  - validation
  - falsification
